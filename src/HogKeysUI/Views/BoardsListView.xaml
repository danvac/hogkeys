<UserControl x:Class="HogKeysUI.Views.BoardsListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.ignore.com"
        mc:Ignorable="d ignore"
        DataContext="{Binding Main, Source={StaticResource Locator}}" MinHeight="0" MinWidth="150">
    <UserControl.Resources>
        <ResourceDictionary Source="/WindowResources.xaml"/>
    </UserControl.Resources>
    <Border 
                Style="{StaticResource MainBorderStyle}">
        <HeaderedContentControl Header="Boards" 
                                Style="{StaticResource MainHCCStyle}">
            <ListBox Margin="10"  
                 ItemsSource="{Binding Path=Boards}" 
                        
                 ItemTemplate="{StaticResource BoardTemplate}" 
                         SelectedItem="{Binding SelectedBoard,Mode=TwoWay}"/>
        </HeaderedContentControl>
    </Border>
</UserControl>
